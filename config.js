// Fill these in once — see README.md for the setup walkthrough.
const CONFIG = {
  // Google Cloud OAuth client ID (YouTube uploads)
  googleClientId: "YOUR_CLIENT_ID.apps.googleusercontent.com",

  // The GitHub repo this site lives in (metadata is saved to moves.json there)
  github: {
    owner: "Ithrendrial",
    repo: "salsa-tutorials",
    branch: "main",
  },
};
